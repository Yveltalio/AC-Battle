#include "attaque.hpp"
#include <vector>
using namespace std;

void AttributionAttaque(std::string nom_perso)
{
    //créer un vector d'attaque
    vector<Attaque> att;
    if (nom_perso == "Bambou")
    {
        //créer une attaque avec la description , le montant de degats etc...
        //l'ajouter dans le vector
        //return le vector
    }
}